<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:background="@color/colorPrimary"
        android:gravity="center">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="vertical">

            <AutoCompleteTextView
                android:id="@+id/departure_entry"
                android:layout_width="250dp"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:layout_alignParentTop="true"
                android:layout_centerHorizontal="true"
                android:drawableStart="@drawable/circle"
                android:hint="Departure Station"
                android:inputType="textFilter"
                android:singleLine="true"
                android:textSize="20sp" />

            <AutoCompleteTextView
                android:id="@+id/destination_entry"
                android:layout_width="250dp"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:layout_below="@id/departure_entry"
                android:layout_centerHorizontal="true"
                android:drawableStart="@drawable/poi"
                android:hint="Destination Station"
                android:inputType="textFilter"
                android:singleLine="true"
                android:textSize="20sp" />

            <ImageButton
                android:id="@+id/reverse"
                android:layout_width="70dp"
                android:layout_height="70dp"
                android:layout_alignBottom="@id/destination_entry"
                android:layout_alignTop="@id/departure_entry"
                android:layout_marginStart="16dp"
                android:layout_toEndOf="@id/destination_entry"
                android:background="@android:color/transparent"
                android:focusableInTouchMode="true"
                android:padding="16dp"
                android:src="@drawable/arrows" />

        </RelativeLayout>

    </Toolbar>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin" />


</LinearLayout>
